{
  "name": "pauth",
  "version": "0.3.0",
  "description": "path-based authorization server",
  "main": "pauth.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemailer": "^6.4.6"
  }
}
